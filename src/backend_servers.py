  # Replace the backend server information with your actual backend servers
  # This can handle IP as well

def setup_backend_servers():
     backend_servers = [("localhost", 80), ("localhost", 81), ("localhost", 82)]
     return backend_servers